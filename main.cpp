#include <iostream>
#include "Mission.h"
#include <string>
int main() {
   /* std::string st = "type A";
    std::string st1 = "type B";
    std::string st2 = "type C";
    double number = 100;
    FILE *fd;
    class Menu::Basic_config basic;
    fd = basic.load_from_file(st);
    Ships::Ship a = Ships::Ship(st, st, number, number, number);
    Ships::Transport_ship b = Ships::Transport_ship(st, st, number, number, number, number, number);
    Ships::Military_transport_ship d = Ships::Military_transport_ship(st2, st2, number, number, number, number, number);
    Ships::Security_ship c = Ships::Security_ship(st1, st1, number, number, number);
   // basic.add_ship(&b);
    //basic.add_ship(&c);
    //basic.add_ship(&d);

    //basic.safe_b(fd); */

  /* Basic::Armament armament;
   std::cin >> armament;
   std::cout << armament;
   armament.change_property("rate of fire", 100000);
    std::cout << armament.get_property("rate of fire");
    std::cout << "Hello, World!" << std::endl;*/
    //class Menu::Mission mission;
    return 0;
}
